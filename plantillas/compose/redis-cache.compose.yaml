# Plantilla: Redis (caché / sesiones)
services:
  redis:
    image: redis:alpine
    container_name: ${PROJECT_NAME:-mi-cache}-redis
    ports:
      - "${HOST_PORT:-6379}:6379"
    restart: unless-stopped
