# Plantilla: Nginx oficial sin construir imagen propia
services:
  web:
    image: nginx:alpine
    container_name: ${PROJECT_NAME:-mi-web}-nginx
    ports:
      - "${HOST_PORT:-8080}:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
    restart: unless-stopped
